<!DOCTYPE html>
<html xmlns="http://www.thymeleaf.org">
	<head>
		<meta charset="UTF-8">
		<title>Cafe List</title>
		<script th:inline="javascript">
			function checkdelete() {
				var checkdelete = confirm("카페들을 모두 삭제하시겠습니까?");
				if(checkdelete) {
					window.location.href='cafes/delete/all';
				}
			}
		</script>
	</head>
<body>
	<h2>Cafe List</h2>
		<table border="1">
			<thead>
				<tr>
					<th>CAFE_ID</th>
					<th>NAME</th>
					<th>LOCATION</th>
					<th>OPENING_HOURS</th>
					<th>CLICK</th>
				</tr>
			</thead>
			<tbody>
				<tr th:each="board : ${boards}">
					<td th:text="${board.boardId}"></td>
					<td th:text="${board.title}"></td>
					<td th:text="${board.author}"></td>
					<td>
					<a th:href="@{/boards/details/{boardId}(boardId=${board.boardId})}">View Details</a>
					<a th:href="@{/boards/update/{boardId}(boardId=${board.boardId})}">수정</a>
					<a th:href="@{/boards/delete/{boardId}(boardId=${board.boardId})}">삭제</a>
					</td>
				</tr>
			</tbody>
			
		</table>
		<a th:href="@{/boards/new}">Add New Board</a>
		<!--<a th:href="@{/boards/delete/all}">모든 게시물 삭제</a>-->
		<a href="#" th:onclick="checkdelete()">모든 게시물 삭제</a>
</body>
</html>