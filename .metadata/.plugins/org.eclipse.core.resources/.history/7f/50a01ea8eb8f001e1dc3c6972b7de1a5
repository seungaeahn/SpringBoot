<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
	<head>
		<meta charset="UTF-8">
		<title>Cafe Exists</title>
		<link rel="stylesheet" href="/css/styles.css" th:href="@{/css/styles.css}"/>
	</head>
<body>
	<h2>Cafe Exists</h2>
	<p th:if="{cafeExists}">
		<span th:text="'Cafe' + ${name} + '존재합니다.'"></span>
	</p>
</body>
</html>